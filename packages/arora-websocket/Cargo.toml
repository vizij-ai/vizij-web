[package]
name = "arora-websocket"
version = "0.1.0"
edition = "2021"
description = "Reusable arora-types WebSocket protocol utilities"

[dependencies]
arora-schema = { git = "https://github.com/semio-ai/arora-types.git" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
