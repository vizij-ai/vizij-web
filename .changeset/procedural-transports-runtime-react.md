---
"@vizij/runtime-react": minor
---

Add transport controls for bundled procedural programs discovered from exported `motiongraph` bundle entries. The runtime now exposes program discovery plus `playProgram`, `pauseProgram`, `stopProgram`, and `getProgramState`, and standalone/browser consumers can surface bundled animations and procedural programs from the loaded asset.
