---
"@vizij/face-core": patch
---

Scaffold `@vizij/face-core` (L1 placeholder): reserves the package name, joins
the fixed release line with `@vizij/runtime-react` and `@vizij/render`, and
holds the API-surface snapshot slot. No runtime code yet — the `FaceRuntime`
extraction lands here per `docs/redesign/06-track-2-implementation.md`.
