---
"@vizij/studio-support": patch
"@vizij/runtime-react": patch
---

Move playable program graph registrations, input constraints, and program reset derivation into the Studio-support runtime registration plan so React runtime hosts consume prepared asset support data instead of rebuilding program graph setup locally.
