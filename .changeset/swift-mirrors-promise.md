---
"@vizij/render": patch
"@vizij/speech-react": patch
---

Publish the speech-enabled authoring and standalone package surfaces, including
the shared speech React hooks package and the renderer bundle typing updates
that support those flows.
